<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="io.github.hohojia886.pixeltweaks.permission.SYNC_SETTINGS" />

    <permission
        android:name="io.github.hohojia886.pixeltweaks.permission.SYNC_SETTINGS"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:restoreAnyVersion="true"
        android:directBootAware="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PixelTweaks">

        <!-- libxposed metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Pixel Tweaks: Custom system modifications." />
        <meta-data
            android:name="xposedminversion"
            android:value="102" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Custom Provider for cross-process preferences -->
        <provider
            android:name=".providers.RemotePrefProvider"
            android:authorities="io.github.hohojia886.pixeltweaks"
            android:exported="true"
            android:directBootAware="true" />

        <receiver
            android:name=".receivers.BootReceiver"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
